name: hello workd

on:
  workflow_dispatch:
  pull_request:
    branches:
      -main
  push:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Display Hello World MEssage
        run : echo "Hello"

      - name: Another Task
        run:  echo "Another task"
          
